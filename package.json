{
  "name": "shared-github-workflows",
  "private": true,
  "scripts": {
    "lint": "eslint '.github/actions/**/*.js'",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@resident-advisor/eslint-config": "*",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
